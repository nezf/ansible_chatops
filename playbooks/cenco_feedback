---
- name: Provide feedback to Network Operator
  hosts: all
  #connection: local
  gather_facts: false
  #vars:
  #  regex_pattern: '\b(\w+\.cenco\.corp)\b'          # can supply via vars.yml
  #  eda_input: "{{ ansible_eda.event.payload.text }}"  # access event payload

  tasks:
    - debug:
        msg: "Fallamos en el proceso"
