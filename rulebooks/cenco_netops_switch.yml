---
- name: Listen for events on a webhook
  hosts: all
  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000
        
  rules:
    - name: Filter payload for hostnames
      condition: event.payload.message contains "Ansible"
      action:
        run_workflow_template:
          name: "Asignación de VLAN"
          organization: Default
